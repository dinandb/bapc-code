#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {

    int n;
    cin >> n;
    vector<int> vec ={};
    int elt;
    // matrix
    // const int N = 5;
    // vector<vector<int>> adj(n, vector<int>(n, 0));
    // can only use mat[N][N] = {0}; if N is known at compile time
    // mat[0][1] = 2;
    for (int i = 0; i < n; i++) {
        cin >> elt;
        vec.push_back(elt);
    }

    return 0;
}
// sortby second entry
// sort(v.begin(), v.end(), [](auto &a, auto &b) {
//     return get<1>(a) < get<1>(b);  // sort by second element
// });
// standard is first entry




hashmap:
#include <functional> // for std::hash

hashmap[hasher(schedule)] = true;

hashmap[hasher(new_col)]

ColumnHasher hasher;

hash<int> hasher;
int a = 5;
hasher (a) = 


#include <cmath>
double ln_x = log(10); // base e
double log10_x = log10(10); // base 10
double result = pow(base, exponent); (all doubles)