#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {

    int n;
    cin >> n;
    vector<int> vec ={};
    int elt;
    for (int i = 0; i < n; i++) {
        cin >> elt;
        vec.push_back(elt);
    }

    return 0;
}
// sortby second entry
// sort(v.begin(), v.end(), [](auto &a, auto &b) {
//     return get<1>(a) < get<1>(b);  // sort by second element
// });
// standard is first entry




hashmap:
#include <functional> // for std::hash

hashmap[hasher(schedule)] = true;

hashmap[hasher(new_col)]

ColumnHasher hasher;

hash<int> hasher;
int a = 5;
hasher (a) = 
