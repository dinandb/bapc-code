#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {

    int n, q;
    cin >> n >> q;
    vector<int> vec ={};
    int elt;
    for (int i = 0; i < n; i++) {
        cin >> elt;
        vec.push_back(elt);
    }

    return 0;
}